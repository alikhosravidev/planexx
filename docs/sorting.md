# راهنمای پیاده‌سازی Sortable Models

سلام! اگه میخواید مدلتون قابلیت مرتب‌سازی و sort داشته باشه، این داکیومنت رو دنبال کنید و قدم به قدم پیش برید 👇

## قدم اول: پیاده‌سازی اینترفیس و تریت
ابتدا باید `SortableEntity` اینترفیس رو implement کنید و تریت `HasSorting` رو به مدلتون اضافه کنید:

```php
use App\Services\SortOrder\Contracts\SortableEntity;
use App\Services\SortOrder\Traits\HasSorting;

class YourModel extends Model implements SortableEntity
{
    use HasSorting;
    
    // ...
}
```

## قدم دوم: تنظیم ستون سورت
اگه اسم ستون سورتتون چیزی غیر از `order` هست، این متد رو override کنید:

```php
public function sortingColumnName(): string
{
    return 'your_sort_column_name'; // مثلاً position یا sort_order
}
```

## قدم سوم: تنظیم scope سورتینگ
اگه نیاز دارید سورتینگ براساس یک scope خاص باشه (مثلاً فقط رکوردهای مربوط به یک category)، متد `baseSortQuery` رو override کنید:

```php
public function baseSortQuery(): Builder
{
    return $this->newQuery()
        ->where('category_id', $this->category_id)
        ->where('is_active', true);
}
```

## امکانات آماده
بعد از این تنظیمات، این قابلیت‌ها رو خواهید داشت:

1. **سورت خودکار رکوردهای جدید**:
    - رکورد جدید به صورت خودکار در آخرین پوزیشن قرار میگیره

2. **جابجایی آیتم‌ها**:

3. **مرتب‌سازی مجدد بعد از حذف**:
    - بعد از حذف یک رکورد، بقیه آیتم‌ها به صورت خودکار مرتب میشن

4. **کوئری براساس سورت**:
```php
YourModel::ordered()->get();
```

## زیرساخت
این سیستم از یک observer استفاده می‌کنه که به صورت خودکار روی مدل‌های Sortable اعمال میشه و نیازی به تنظیم اضافه نداره.

امیدوارم این راهنما کمکتون کرده باشه! اگه مشکلی هم پیش اومد، حتماً بپرسید ازم 😊
