# 🔗 گزارش لینک‌دهی ماژول پایگاه تجربه سازمانی

## ✅ تغییرات انجام شده

### 1️⃣ داشبورد اصلی (`/dashboard/index.php`)
- ✅ لینک ماژول در Quick Access به `/dashboard/knowledge/index.php` تغییر کرد
- ✅ آمار تجربه‌ها از `432` به `184` تغییر کرد (مطابق با داده‌های ماژول)
- ✅ آیکون تجربه‌ها به `fa-lightbulb` و رنگ به `yellow` تغییر کرد

### 2️⃣ سایدبار داشبورد (`/_components/dashboard-sidebar.php`)
- ✅ نام ماژول از "پایگاه دانش سازمانی" به "پایگاه تجربه سازمانی" تغییر کرد
- ✅ لینک به `/dashboard/knowledge/index.php` تصحیح شد
- ✅ آیکون `fa-book` حفظ شد

### 3️⃣ صفحه اصلی (`/index.php`)
تمام صفحات ماژول به لیست پروتوتایپ‌ها اضافه شد:
- ✅ داشبورد ماژول
- ✅ لیست تجربیات
- ✅ ثبت تجربه جدید
- ✅ قالب‌های تجربه
- ✅ افزودن قالب تجربه

### 4️⃣ صفحه تست لینک‌ها (`/test-links.html`)
- ✅ صفحه جامع برای تست تمام لینک‌ها ایجاد شد
- ✅ دسته‌بندی شده در بخش‌های مختلف
- ✅ شامل توضیحات و راهنما

---

## 📍 ساختار مسیرها

### مسیر اصلی ماژول
```
/dashboard/knowledge/
├── index.php                          → داشبورد ماژول
├── experiences/
│   ├── list.php                       → لیست تجربیات
│   ├── create.php                     → ثبت تجربه جدید
│   ├── edit.php                       → ویرایش تجربه (پیاده‌سازی نشده)
│   └── view.php                       → مشاهده تجربه (پیاده‌سازی نشده)
└── templates/
    ├── list.php                       → لیست قالب‌ها
    ├── create.php                     → افزودن قالب جدید
    ├── edit.php                       → ویرایش قالب (پیاده‌سازی نشده)
    └── view.php                       → مشاهده قالب (پیاده‌سازی نشده)
```

---

## 🔄 مسیرهای لینک‌دهی

### از داشبورد اصلی
```
داشبورد اصلی (/dashboard/index.php)
└── کلیک روی کارت "پایگاه تجربه سازمانی"
    └── هدایت به /dashboard/knowledge/index.php
```

### از سایدبار
```
هر صفحه‌ای در سیستم
└── سایدبار → "پایگاه تجربه سازمانی"
    └── هدایت به /dashboard/knowledge/index.php
```

### داخل ماژول
```
/dashboard/knowledge/index.php (داشبورد ماژول)
├── کارت "مدیریت تجربیات" → /dashboard/knowledge/experiences/list.php
├── کارت "قالب‌های تجربه" → /dashboard/knowledge/templates/list.php
└── کارت "ثبت تجربه جدید" → /dashboard/knowledge/experiences/create.php
```

---

## 🎯 Breadcrumbs تمام صفحات

### صفحات تجربیات
```
لیست تجربیات:
خانه > پایگاه تجربه سازمانی > مدیریت تجربیات

ثبت تجربه:
خانه > پایگاه تجربه سازمانی > مدیریت تجربیات > ثبت تجربه جدید
```

### صفحات قالب‌ها
```
لیست قالب‌ها:
خانه > پایگاه تجربه سازمانی > قالب‌های تجربه

افزودن قالب:
خانه > پایگاه تجربه سازمانی > قالب‌های تجربه > افزودن قالب جدید
```

---

## ✨ ویژگی‌های لینک‌دهی

### 1. Breadcrumbs کلیک‌پذیر
همه سطوح breadcrumb (به جز آخرین مورد) کلیک‌پذیر هستند:
```php
['label' => 'خانه', 'url' => '/dashboard/index.php'],
['label' => 'پایگاه تجربه سازمانی', 'url' => '/dashboard/knowledge/index.php'],
['label' => 'قالب‌های تجربه', 'url' => '/dashboard/knowledge/templates/list.php'],
['label' => 'افزودن قالب جدید'], // غیرفعال
```

### 2. دکمه‌های بازگشت
تمام صفحات فرم‌ها دکمه "بازگشت به لیست" دارند:
```php
$actionButtons = [
    ['label' => 'بازگشت به لیست', 'url' => '...', 'icon' => 'fa-arrow-right', 'type' => 'outline']
];
```

### 3. سایدبار ماژول
سایدبار اختصاصی ماژول (`knowledge-sidebar.php`):
- لینک بازگشت به داشبورد اصلی
- لینک به داشبورد ماژول
- لینک به لیست تجربیات
- لینک به قالب‌های تجربه

---

## 🧪 نحوه تست

### مرحله 1: تست از صفحه اصلی
1. باز کردن `/index.php`
2. کلیک روی کارت "ماژول پایگاه تجربه سازمانی"
3. باید به `/dashboard/knowledge/index.php` هدایت شود

### مرحله 2: تست از داشبورد
1. باز کردن `/dashboard/index.php`
2. کلیک روی کارت "پایگاه تجربه سازمانی" در Quick Access
3. باید به داشبورد ماژول هدایت شود

### مرحله 3: تست سایدبار
1. در هر صفحه سیستم
2. کلیک روی "پایگاه تجربه سازمانی" در سایدبار
3. باید به داشبورد ماژول هدایت شود

### مرحله 4: تست صفحه مخصوص
باز کردن `/test-links.html` برای:
- مشاهده تمام لینک‌ها در یک صفحه
- تست سریع همه مسیرها
- بررسی صحت لینک‌دهی

---

## 📊 خلاصه فایل‌های تغییر یافته

| فایل | تغییرات |
|------|---------|
| `/dashboard/index.php` | تصحیح لینک و آمار ماژول تجربه |
| `/_components/dashboard-sidebar.php` | تصحیح نام و لینک ماژول |
| `/index.php` | افزودن تمام صفحات ماژول به لیست پروتوتایپ |
| `/test-links.html` | ایجاد صفحه تست جامع (جدید) |

---

## ✅ چک‌لیست نهایی

- [x] لینک در داشبورد اصلی
- [x] لینک در سایدبار اصلی
- [x] لینک در صفحه اصلی پروتوتایپ
- [x] Breadcrumbs تمام صفحات
- [x] دکمه‌های بازگشت
- [x] سایدبار ماژول
- [x] لینک‌های داخلی ماژول
- [x] صفحه تست لینک‌ها

---

## 🚀 آماده برای استفاده

تمام لینک‌دهی‌ها به درستی پیکربندی شده‌اند و پروتوتایپ آماده استفاده است!

برای شروع تست:
1. باز کردن `/test-links.html`
2. کلیک روی هر لینک برای اطمینان از صحت عملکرد
3. بررسی مسیریابی و breadcrumbs

---

**تاریخ به‌روزرسانی**: 2025/09/05  
**وضعیت**: ✅ تکمیل شده و آماده استفاده
